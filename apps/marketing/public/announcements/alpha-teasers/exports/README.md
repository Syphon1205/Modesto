# Teaser MP4s

Generated by `apps/marketing/scripts/capture-alpha-teaser.mjs` (Playwright +
ffmpeg) when you press Download in the preview, or from the CLI.

Filenames: `modesto-alpha-{slug}-{ratio}-{theme}.mp4`. Signal cuts include
`dark` or `light`. Other styles are dark-only and omit the theme suffix.
